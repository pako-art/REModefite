org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.daemon=false

# Platform
minecraft_version=1.21.1
neoforge_version=21.1.248

# Mod Properties
# modId stays "modefite": ClientInitializer derives the resource folder
# "<modid>_items_override" from it, so renaming would break packs that use it.
mod_id=modefite
mod_version=1.0.1+1.21.1
maven_group=timmychips.modefiteitemdefinitions
archives_base_name=Modefite-NeoForge
